import React from 'react'

export default function Profile({ params }: { params: { profile: string } } ) {

  // TODO: get profile using subdirectory 
  // TODO: make edit button visible only when session user id = retrieved profile user id

  return (
    <div>Profile</div>
  )
}